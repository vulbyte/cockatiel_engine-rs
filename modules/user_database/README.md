# users module

this module passively scans each module for a command that needs user, and will append the user data to the user slot, additionally if a module requests user data this is the module to fufil it
